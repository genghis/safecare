# SafeCare Captive Portal DNS + DHCP
# All DNS queries resolve to the Pi's AP address (captive portal)
interface=wlan0
bind-interfaces
dhcp-range=10.42.0.10,10.42.0.100,255.255.255.0,24h
address=/#/10.42.0.1
